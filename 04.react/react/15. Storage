WebStorage

- loacl / session

- loalStorage : k : v, 문자열로 저장, JSON

- 사용
window.loalStorage.

    - setItem(k : v)
    - getItem(k)
    - removeItem()
    - clear()
    - length
    - key(idx) : 로컬 스토리지의 인덱스 번호로 키 값을 출력해주는 메소드
